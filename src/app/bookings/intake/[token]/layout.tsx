export const metadata = {
  title: 'Intake | LoveCry Counselling',
  description: 'Complete your confidential LoveCry counselling intake form.',
};

export default function IntakeLayout({ children }: { children: React.ReactNode }) {
  return children;
}
